import { Story, Meta } from '@storybook/react'
import TechIcons from '.'

export default {
  title: 'Components/TechIcons',
  component: TechIcons
} as Meta

export const Default: Story = () => <TechIcons />
